{
  "variables": {
    "aws_access_key": "",
    "aws_secret_key": "",
    "basename": "wordpress",
    "environment": "Pro",
    "subnet": "Public Subnet A",
    "timezone": "Europe/Madrid"
  },
  "builders": [
    {
      "type": "amazon-ebs",
      "access_key": "{{user `aws_access_key`}}",
      "secret_key": "{{user `aws_secret_key`}}",
      "region": "eu-west-1",
      "instance_type": "t2.micro",
      "ssh_username": "ubuntu",
      "source_ami_filter": {
        "filters": {
          "virtualization-type": "hvm",
          "name": "ubuntu/images/*20.04-amd64-server-*",
          "root-device-type": "ebs"
        },
        "owners": ["099720109477"],
        "most_recent": true
      },
      "vpc_filter": {
        "filters": {
          "tag:Name": "{{user `basename`}}"
        }
      },
      "subnet_filter": {
        "filters": {
          "tag:Name": "{{user `subnet`}}"
        }
      },
      "ami_name": "{{user `basename`}}-docker {{timestamp}}",
      "tags": {
        "Name": "{{user `basename`}}-docker {{timestamp}}",
        "Env": "{{user `environment`}}"
      },
      "snapshot_tags": {
        "Name": "{{user `basename`}}-docker {{timestamp}}",
        "Env": "{{user `environment`}}"
      }
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "script": "./ubuntu-setup.sh",
      "environment_vars": [ "TIMEZONE={{user `timezone`}}" ],
      "execute_command": "sudo sh -c '{{ .Vars }} {{ .Path }}'"
    },
    {
      "type": "file",
      "source": "./key.pub",
      "destination": "/home/ubuntu/.ssh/authorized_keys"
    },
    {
      "type": "file",
      "source": "docker",
      "destination": "/home/ubuntu"
    },
    {
      "type": "file",
      "source": "scripts",
      "destination": "/home/ubuntu"
    },
    {
      "type": "shell",
      "inline": "sudo /bin/sh -c 'mv /home/ubuntu/docker /home/ubuntu/scripts /data'"
    },
    {
      "type": "shell",
      "inline": "sudo chown -R root.root /data"
    }
  ]
}
